import { createBaseAccountSDK } from '@base-org/account';

export const sdk = createBaseAccountSDK({
  appName: 'AI Image Transformer',
  appLogo: 'https://base.org/logo.png', // You can replace with your own logo
});